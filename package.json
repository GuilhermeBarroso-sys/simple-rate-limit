{
  "name": "redis-api-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --transpile-only --ignore-watch node_modules index.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Simple Express rate limiting middleware using Redis for IP-based request throttling.",
  "devDependencies": {
    "typescript": "^5.8.2",
    "ts-node-dev": "^2.0.0",
    "@types/express": "^5.0.1",
    "@types/node": "^22.13.11"

  },
  "dependencies": {
    "axios": "^1.8.4",
    "express": "^4.21.2",
    "ioredis": "^5.6.0"
  }
}
